TRACEPOINT_EVENT(osd, // provider的名称 
do_osd_op_pre_copy_get,// tracepoint的名称
    TP_ARGS(// 输入参数列表
        const char*, oid,
        uint64_t, snap),
    TP_FIELDS( // 输出列表
        ctf_string(oid, oid)
        ctf_integer(uint64_t, snap, snap)
    )
)

